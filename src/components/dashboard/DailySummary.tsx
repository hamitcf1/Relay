import { Banknote, CheckCircle2, ChevronRight, CircleDollarSign, ClipboardList } from 'lucide-react'

export function DailySummary({ activeCount, saleCount, collectedCount, awaitingCount, copy, onOpenSales }: { activeCount: number; saleCount: number; collectedCount: number; awaitingCount: number; copy: Record<string, string>; onOpenSales: () => void }) {
    const rows = [{ label: copy.openRecords, value: activeCount, Icon: ClipboardList, tone: '' }, { label: copy.sales, value: saleCount, Icon: CircleDollarSign, tone: '' }, { label: copy.collected, value: collectedCount, Icon: CheckCircle2, tone: 'text-emerald-600' }, { label: copy.awaiting, value: awaitingCount, Icon: Banknote, tone: awaitingCount ? 'text-amber-600' : '' }]
    return <section aria-labelledby="daily-status-title" className="rounded-2xl border border-border/70 bg-card p-4 shadow-sm sm:p-5"><div className="flex items-center justify-between"><h2 id="daily-status-title" className="text-base font-semibold tracking-tight">{copy.today}</h2><button onClick={onOpenSales} className="flex items-center gap-1 text-xs font-semibold text-primary">{copy.openSales}<ChevronRight className="size-4" /></button></div><div className="mt-3 divide-y divide-border/60">{rows.map(({ label, value, Icon, tone }) => <div key={label} className="grid grid-cols-[auto_1fr_auto] items-center gap-3 py-3"><span className={`grid size-9 place-items-center rounded-xl bg-muted ${tone || 'text-muted-foreground'}`}><Icon className="size-4" /></span><span className="text-sm text-muted-foreground">{label}</span><strong className="text-lg font-semibold tabular-nums">{value}</strong></div>)}</div></section>
}
